package OCA.interfaces;

public interface Drawable {

    //an interface is a contract that a class can implement to guarantee that it will provide certain behavior
    //interface are public by default and abstract
    //interface can have abstract methods and static methods
    //interface can have default methods
    //interface can have static methods
    //interface can have private methods
    //interface can have private static methods
    //interface can have private default methods
    //interfaces can only contain variables that are public, static and final. these three modifiers are optional

    // no instances of an interface can be created
    // an interface defines behaviors that a class must implement
    // a class can implement multiple interfaces. an unlimited number of interfaces can be implemented by a class
    // an interface can extend another interface





}
